services:
  web:
    build: .
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DJANGO_DEBUG=True
    command: >
      sh -c "python manage.py makemigrations books &&
             python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
